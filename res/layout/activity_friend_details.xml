<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".FriendDeailsActivity" >

    <ImageView
        android:id="@+id/imgPhoto"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:src="@drawable/help" />

    <TextView
        android:id="@+id/tvName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/imgPhoto"
        android:layout_marginLeft="48dp"
        android:layout_toRightOf="@+id/imgPhoto"
        android:text="Medium Text"
        android:textAppearance="?android:attr/textAppearanceMedium" />

    <ToggleButton
        android:id="@+id/tgBlkBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tvName"
        android:layout_below="@+id/tvName"
        android:textOn="BLOCK"
        android:textOff="UN-BLOCK"
        android:text="ToggleButton" />

    <FrameLayout
        android:id="@+id/frd_details_frame"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/imgPhoto"
        android:layout_below="@+id/imgPhoto"
        android:layout_marginTop="30dp" >

    </FrameLayout>

    <Button
        android:id="@+id/btnNewReminder"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tgBlkBtn"
        android:layout_marginLeft="20dp"
        android:layout_toRightOf="@+id/imgPhoto"
        android:visibility="gone"
        android:text="Send New Reminder" />

</RelativeLayout>